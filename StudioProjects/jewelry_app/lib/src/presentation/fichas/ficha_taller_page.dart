import 'package:flutter/material.dart';

class FichaTallerPage extends StatelessWidget {
  const FichaTallerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ficha Taller'),
      ),
      body: const Center(
        child: Text('Ficha Taller'),
      ),
    );
  }
}
